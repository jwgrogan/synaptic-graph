{
  "identifier": "default",
  "description": "Default capabilities for Memory Galaxy",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "shell:allow-open",
    "dialog:allow-open"
  ]
}
